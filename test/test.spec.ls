describe 'A test suite' !->
  specify 'it should succeed' !->
    expect true .to .be .true
  specify 'it should succeed again' !->
    expect true .to .be .true
  specify 'it should succeed now' !->
    expect true .to .be .true
